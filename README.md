# anyangle

Work in progress any-angle pathfinding over polygons with holes.

For now, we only have monotone decomposition of polygon with holes implemented.

## Demo

To see `anyangle` in action, run our graphical user interface demo:

```sh
cargo run --manifest-path=demo/Cargo.toml
```
